rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // This rule allows anyone to read and write to Storage.
    // WARNING: This is NOT secure for production.
    // It is ONLY for local emulator testing.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}